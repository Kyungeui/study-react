import React from 'react';

/** scss파일 참조하기 --> 참조변수 사용 안함 */

/**
 * SCSS를 사용하는 컴포넌트
 * 
 * 패키지 설치 필요
 *      yarn add node-sass
 */
const Scss = () => {
    return (
        <div>
            <h2>Scss</h2>
            
        </div>
    );
};

export default Scss;